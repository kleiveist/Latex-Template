\begin{figure}[H]
  \centering
  \resizebox{0.98\textwidth}{!}{%
  \begin{tikzpicture}[node distance=8mm and 9mm]
    \node[casePrimary,minimum width=42mm] (repository) {Master-Repository};

    \node[caseBox,minimum width=42mm,below left=of repository] (profiles)
      {\textbf{profiles/} + \textbf{config/}\\Auswahl und Variablenvertrag};
    \node[caseBox,minimum width=42mm,below right=of repository] (tools)
      {\textbf{tools/}\\Generierung und Lebenszyklus};

    \node[caseBox,minimum width=42mm,below left=12mm and 53mm of repository] (frontend)
      {\textbf{frontend/}\\UI, Build, HTTP-Client};
    \node[caseBox,minimum width=42mm,right=of frontend] (backend)
      {\textbf{backend/}\\API, Konfiguration, optionale DB};
    \node[caseBox,minimum width=42mm,right=of backend] (tauri)
      {\textbf{src-tauri/}\\native Desktop-Grenze};

    \node[caseBox,minimum width=42mm,below=of frontend] (shared)
      {\textbf{shared/}\\Schema, Beispiele, Assets};
    \node[caseBox,minimum width=42mm,right=of shared] (deployment)
      {\textbf{deployment/}\\providerneutrale Artefakte};
    \node[caseBox,minimum width=42mm,right=of deployment] (docs)
      {\textbf{docs/}\\Architektur und Betrieb};

    \draw[caseFlow] (repository) -- (profiles);
    \draw[caseFlow] (repository) -- (tools);
    \draw[caseSecondaryFlow] (profiles) -- node[left,font=\sffamily\tiny] {wählt} (frontend);
    \draw[caseSecondaryFlow] (profiles) -- (backend);
    \draw[caseSecondaryFlow] (profiles) -| (tauri);
    \draw[caseSecondaryFlow] (tools) -- node[right,font=\sffamily\tiny] {steuert} (backend);
    \draw[caseSecondaryFlow] (tools) -| (frontend);
    \draw[caseSecondaryFlow] (tools) -| (tauri);
    \draw[caseSecondaryFlow] (tools) -- node[above,font=\sffamily\tiny] {validiert} (shared);
    \draw[caseSecondaryFlow] (deployment) -- (backend);
    \draw[caseSecondaryFlow] (tauri) -- node[above,font=\sffamily\tiny] {hostet} (frontend);

    \begin{scope}[on background layer]
      \node[caseGroup,fit=(repository)(profiles)(tools)(frontend)(backend)(tauri)(shared)(deployment)(docs),
        label={[font=\sffamily\bfseries\scriptsize,text=caseBlue]above:Verantwortungsbereiche im Repository}] {};
    \end{scope}
  \end{tikzpicture}}
  \caption{Komponenten und Verantwortungsgrenzen im Master Repository}
  \label{fig:component-architecture}
  \smallskip
  {\footnotesize Quelle: Eigene Darstellung auf Grundlage von
  \textcite{kleiveist2026architecture}.\par}
\end{figure}
